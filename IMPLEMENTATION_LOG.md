# Anime browser implementation

- Novel Themes 1.3.3 (2026-09-07): read the user's Beeza Novel Studio and BeezaChat Novel story references; generated twelve distinct character scenes through built-in ImageGen. Identity references and complete prompts are recorded in `docs/NOVEL_THEMES.md` and `docs/novel-theme-generation.json`; original PNGs remain under `artwork-source/novel-themes/`. The offline application contains 12 wallpapers plus 12 thumbnails in WebP (2,694,028 bytes total).
- Shared `src/themes.js` catalog supplies renderer palettes, validated profile identifiers, and native Windows caption colors. Four light and eight dark palettes cover app chrome, internal pages, Calendar, forms, dialogs and panels. Guest web contents are untouched. Theme Studio exposes new/light/dark/classic filters, previews, swatches, selection state, random selection excluding the active theme, and the incumbent customization controls. Existing twelve themes and schema 4 data remain compatible.
- Validation: syntax checks and 18 unit tests passed; all 19 source Electron tests passed, including real selection/restart, artwork controls, guest style isolation, and screenshots at 1440×900 / 1024×768. Palette foreground/background contrast meets 4.5:1 for normal/secondary text and button labels, with focus color at least 3:1. One batched visual inspection plus one confirmation fixed panoramic cropping and light-theme icon/button contrast; Impeccable detect returned no findings.

- Local Calendar 1.3.2 (2026-09-07): added a native internal `cherry://calendar` page, sidebar and command palette entries, month/agenda views, date navigation, search, editable timed/all-day/multi-day appointments, categories, location/details and opt-in Windows reminders. Calendar data is stored in schema 4 with a pre-migration backup; all-day dates remain local date strings and timed events use timestamps. Reminders run with the existing 15-second scheduler while Cherry is open, fire once, and catch unexpired appointments on restart. No calendar account or network service is used.
- Calendar verification: syntax passed; unit 15/15; source Electron suite 16/16; final Calendar suite 4/4; native lifecycle passed; packaged Calendar/navigation/split/Organizer 7/7. Visually inspected the calendar at 1920×1080, 1440×900, 1366×768 and 1024×768, plus agenda and event form; calendar rows fit each viewport and crowded days expose a count with all events in the selected-day panel. Package verification matched all 52 source/asset files. Built `release-calendar/Cherrywebbrowser-1.3.2-portable.exe` and changed `Start-Cherry.cmd` to prefer this release. The old app closed normally and the updated app opened with `--calendar`; normal-profile schema migration preserved notes, post-its, reminders, bookmarks, settings and encrypted secrets, with zero test appointments in the user profile.
- Classic Notes 1.3.1: restored the pre-handoff 1.3 shell/home layout requested by the user while retaining the browser engine and later backend/security work. Added schema 3 storage for Notes, color-coded/pinnable Post-its, and dated Reminders with complete/edit/delete/snooze states. A 15-second main-process scheduler sends native Windows notifications while the app is running and catches overdue items on next launch; schema 2 data is backed up to `cherry-data.json.before-schema-3` before migration.
- Classic Notes verification: syntax and 10/10 unit tests passed; source Electron E2E 12/12 passed after updating classic layout expectations, including Post-it/Reminder UI persistence across restart. Native lifecycle passed, required-size native screenshots passed, and the packaged 1.3.1 navigation plus Organizer scenarios passed 2/2. Package verification matched all 49 shipped source/asset files byte-for-byte. The normal user profile migrated to schema 3 with a backup and the visible packaged app opened successfully.
- UX/UI handoff 1.4.0: rebuilt the existing Electron Home/New Tab composition around the supplied `target-home.png` while preserving the native `WebContentsView` browser engine, routing, state, storage, OAuth, tabs, navigation, downloads, bookmarks, history, notes, workspaces, and security preferences. Added a responsive 240px shell (220px at 1366 and automatic compact 72px at 1024), 44px tab strip, and 56px toolbar. Home now uses real stored history/tabs/workspaces/shortcuts and explicit disconnected AI state instead of reference-image sample data.
- Artwork 1.4.0: generated two separate production illustrations with the built-in ImageGen tool using the supplied targets only as identity/mood references. `cherry-home-short` and `cherry-assistant-short` both keep Cherry's chin-length blue bob and contain no UI text, browser chrome, fruit, shields, keys, padlocks, logo, or watermark. CYRVOR uses a deterministic crop of the supplied brand reference. These are matte artwork panels, not claimed as transparent cutouts.
- Verification 1.4.0: syntax passed; unit 9/9; source Electron E2E 11/11; direct native lifecycle passed; selected packaged navigation/shortcut/screenshot scenarios 3/3. The packaged app also loaded `https://example.com/` in its isolated native `WebContentsView` and captured the unobstructed result. Actual packaged-window screenshots were captured at 1920×1080, 1672×941, 1440×900, 1366×768, and 1024×768. Package verification matched all 49 shipped source/asset files byte-for-byte against `app.asar`.
- Theme Studio 1.3.0: generated nine original 16:9 environment themes with the built-in ImageGen tool using the existing Electric Midnight art as a style reference: Rose Neon, Aurora Glass, Solar Drift, Crimson Circuit, Emerald Grid, Cosmic Archive, Aqua Horizon, Lunar Station, and Silver Fog. Each source PNG is retained under `artwork-source/`; deterministic Sharp export produces a 1672×941 WebP backdrop and 480×270 WebP thumbnail for every theme. Theme Studio now has 12 selectable themes, each new tile applies its matching backdrop and CSS palette, and the backdrop remains independently selectable. No generated scene contains characters, readable text, logos, watermarks, UI, fruit/foliage, shields, keys, or padlocks.
- Theme verification: all nine generation outputs and the in-app 12-tile catalog were visually inspected. Syntax and unit 9/9 passed; source Electron functional suite 10/10 passed including a new theme asset/palette test and native captures of the catalog and Rose home page. The theme test also passes on packaged 1.3.0; all 45 shipped source/asset files match `app.asar` byte-for-byte. Portable size is 103,528,477 bytes.
- OAuth AI 1.2.0: added a provider-agnostic OAuth 2.0 Authorization Code + PKCE flow for user/organization OpenAI-compatible backends. Authorization opens in the system browser, the callback binds only to a random `127.0.0.1` port, `state` and PKCE are verified, duplicate callbacks are rejected, and token exchange/refresh stay in the main process. Access and refresh tokens are encrypted with Electron safeStorage and are never included in renderer state; changing the OAuth resource/client/endpoints invalidates the stored session. Client secrets are intentionally unsupported for this native public client. OpenAI's public API continues to use API-key authentication rather than a simulated ChatGPT OAuth login.
- OAuth verification: syntax 8 modules and unit 9/9 passed; source Electron functional suite 9/9 passed; the OAuth E2E also passed against the packaged executable. The test covers settings UI switching, authorization URL/PKCE fields, loopback callback, code exchange, automatic refresh, Bearer use, local logout, and absence of plaintext tokens from disk/renderer. Version 1.2.0 portable package built and 27 source/asset files match `app.asar` byte-for-byte.
- Scope: implement the supplied Cherry_Browser_Codex_Handoff in the existing Electron browser; no framework migration.
- Instructions: no AGENTS.md found in workspace/parents; read all six requested handoff documents and all seven reference images. Follow AGENTS_APPEND_TH.md as supplied project requirements.
- Baseline: Electron 44.2.0, plain HTML/CSS/JS, WebContentsView, npm/electron-builder. Existing check, four unit tests, and native Electron E2E passed before edits.
- No .git exists. Original project source/assets/tests/scripts copied to `backups/before-anime-20260906-195809`. Browser user profile remains untouched during development.
- Existing data file: app.getPath('userData')/cherry-data.json. Keep this path and migrate with backup/schema version. Test with CHERRY_TEST_PROFILE only.
- Plan: preserve native engine; extend core storage and main lifecycle; retouch supplied art; build navy shell/top tabs/hero/panels; connect workspaces/split/commands/notes/reader/theme/memory/providers; native regression and screenshots at 1920x1080, 1672x941, 1440x900, 1366x768; package and report.
- No configured provider in repo/environment was found. AI must show configuration needed until endpoint/model supplied. Test adapter contracts separately with a labeled local fixture.
- Reference identity: Cherry blue hair, Violet purple hair, Ghost silver-haired man with glasses, Nova white-haired woman with black/red hair accessories. Runtime artwork must remove all fruit, foliage, shields/keys/padlocks and clothing emblems.
- Artwork completed: three generated/retouched source images plus deterministic exports, 14 runtime WebP assets totaling 928,216 bytes. Rejected checkerboard output is not shipped. Main hero is an intentional navy matte panel, not an alpha cutout.
- Native shell implemented, provider adapters connected, storage upgraded with migration backup. Source syntax check and seven unit tests passed; seven Electron integration scenarios passed on the packaged app. The native lifecycle test additionally runs without CDP to verify actual suspension, because instrumented child pages report capture activity.
- Screenshots reviewed against the target and refined for hair/face/hand crops, panel inputs, Thai fonts, and split focus. DPI simulation found one-pixel rounding differences; shell/native layout widths now share the main-process calculation. Package version 1.1.0 is built. Online HTTPS, Open-Meteo, notification request/revocation and package verification have passed. See TEST_REPORT.md and docs JSON evidence for scope and remaining limits.
- Final screenshot matrix and scaling rerun passed after the last CSS adjustment. All 26 packaged source/asset files match the workspace byte-for-byte. Normal interactive app version 1.1.0 launched from release/win-unpacked after closing test instances; user data follows the preserved profile/migration path.
- Audio fix 1.1.1: reproduced the user's screenshot error with real non-silent HTMLAudioElement playback. Electron 44 passes audible on the first event argument; corrected the listener without suppressing production exceptions. New regression fails on old code and passes on source and packaged app. Syntax check, seven unit tests and seven packaged functional tests pass. No profile/schema/partition changes. Details: docs/AUDIO_FIX_TH.md.
- Version 1.1.1 portable build and verification completed: 26 source/asset files match app.asar. Launched normal-profile unpacked app, PID 70184, visible Cherrywebbrowser window responding. Previous portable 1.1.0 retained.
